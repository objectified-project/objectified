"""Tests for personal access token list, create, and revoke commands."""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="tokens commands removed from /v1 CLI registration")
