"""Tests for linked integration list and show commands."""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="integrations commands removed from /v1 CLI registration")
