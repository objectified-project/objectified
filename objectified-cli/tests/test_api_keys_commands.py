"""Tests for workspace API key list, show, create, rotate, and revoke commands."""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="api-keys commands removed from /v1 CLI registration")
