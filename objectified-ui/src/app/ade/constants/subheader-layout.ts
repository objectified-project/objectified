/**
 * Vertical space reserved for fixed ADE subheaders (Studio / Database / Migration toolbars)
 * below {@link TopHeader} (`h-12` = 48px). Must stay in sync with those headers’ real height
 * (`py-1.5`, controls, border) — using 48px here caused ~8px of canvas/content overlap.
 */
export const ADE_SUBHEADER_RESERVE_PX = 58;
